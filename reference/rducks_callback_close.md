# Close an Rducks callback token

Close an Rducks callback token

## Usage

``` r
rducks_callback_close(callback)
```

## Arguments

- callback:

  A callback returned by
  [`rducks_callback()`](https://sounkou-bioinfo.github.io/Rducks/reference/rducks_callback.md).

## Value

`NULL`, invisibly.
