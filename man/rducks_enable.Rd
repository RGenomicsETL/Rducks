% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connection.R
\name{rducks_enable}
\alias{rducks_enable}
\title{Enable Rducks on a DuckDB connection}
\usage{
rducks_enable(
  con,
  extension_path = rducks_extension_path(),
  threads = c("unchanged", "single")
)
}
\arguments{
\item{con}{A \code{duckdb_connection}.}

\item{extension_path}{Extension path. Defaults to \code{\link[=rducks_extension_path]{rducks_extension_path()}}.}

\item{threads}{Either \code{"unchanged"} or \code{"single"}.}
}
\value{
\code{con}, invisibly.
}
\description{
Loads the bundled Rducks DuckDB extension. The registration-safe R UDF path
requires R API work to happen on the recorded main R thread; pass
\code{threads = "single"} to set \code{external_threads=1} and \verb{PRAGMA threads=1}
explicitly. \code{rducks_enable()} also sets DuckDB's
\code{arrow_lossless_conversion=true} option on the user connection; the extension
applies the same setting to its internal connections so DuckDB-specific Arrow
metadata is preserved for typed scalar-UDF, table, and query-stream
marshalling. Use \code{\link[=rducks_set_execution_plan]{rducks_set_execution_plan()}}
before scalar-UDF registration to select a non-reference marshalling or
concurrency plan.
}
\examples{
\donttest{
db <- duckdb::dbConnect(duckdb::duckdb())
rducks_enable(db)
rducks_release(db)
DBI::dbDisconnect(db)
}
}
