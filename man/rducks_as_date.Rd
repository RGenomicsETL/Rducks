% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/time_converters.R
\name{rducks_as_date}
\alias{rducks_as_date}
\alias{rducks_as_timestamp}
\alias{rducks_as_time}
\alias{rducks_as_interval}
\alias{rducks_interval_between}
\title{Convert R date/time values to Rducks scalar-UDF shapes}
\usage{
rducks_as_date(x, tz = "UTC", origin = "1970-01-01")

rducks_as_timestamp(x, tz = "UTC", origin = "1970-01-01")

rducks_as_time(x, tz = "UTC")

rducks_as_interval(
  x,
  units = c("secs", "mins", "hours", "days", "weeks"),
  months = 0L,
  days = 0L
)

rducks_interval_between(start, end, tz = "UTC")
}
\arguments{
\item{x, start, end}{R date/time value. Supported inputs include \code{Date},
\code{POSIXct}/\code{POSIXlt}, \code{difftime}, numeric seconds where documented, and
character strings accepted by base R or \code{HH:MM:SS[.ffffff]} for times.
Numeric \code{DATE}/\code{TIMESTAMP} inputs must be finite or missing. DuckDB \code{TIME}
inputs must be finite seconds in \verb{[0, 86400)} or missing.}

\item{tz}{Time zone used when converting date-times.}

\item{origin}{Origin for numeric timestamp/date input.}

\item{units}{Units for numeric interval input.}

\item{months, days}{Extra interval month/day components for
\code{rducks_as_interval()}.

Numeric and \code{difftime} intervals are rounded to the nearest microsecond before
constructing \code{rducks_interval()}.}
}
\value{
\code{rducks_as_date()} returns a \code{Date} vector; \code{rducks_as_time()} returns
numeric seconds since midnight; \code{rducks_as_timestamp()} returns \code{POSIXct};
\code{rducks_as_interval()} and \code{rducks_interval_between()} return
\code{rducks_interval}.
}
\description{
These helpers normalize common R date/time inputs to the R value shapes used
by Rducks DuckDB scalar-UDF marshalling for \code{DATE}, \code{TIME}, \code{TIMESTAMP}, and
\code{INTERVAL} values.
}
