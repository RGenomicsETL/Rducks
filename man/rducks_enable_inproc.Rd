% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connection.R
\name{rducks_enable_inproc}
\alias{rducks_enable_inproc}
\title{Enable in-process queued R UDF execution}
\usage{
rducks_enable_inproc(con, threads = NULL, external_threads = NULL)
}
\arguments{
\item{con}{A \code{duckdb_connection} already enabled with \code{\link[=rducks_enable]{rducks_enable()}}.}

\item{threads}{Optional positive integer to set with \verb{PRAGMA threads} before
enabling the in-process backend. Use \code{NULL} to leave unchanged.}

\item{external_threads}{Optional positive integer to set with
\verb{SET external_threads} before enabling the in-process backend. Use \code{NULL}
to leave unchanged. For actual DuckDB worker concurrency, keep this smaller
than \code{threads} (for example \verb{threads = 4, external_threads = 1}).}
}
\value{
\code{con}, invisibly.
}
\description{
Switches a Rducks-enabled DuckDB connection to the in-process queued R UDF
backend. This backend preserves R's thread discipline: DuckDB worker-side UDF
callbacks submit chunk requests to an extension-owned queue, and the recorded
main R execution lane drains the queue and performs all R API work. This is a
same-process scheduling mode, not a performance promise; R function calls are
still serialized on the main R thread. This helper changes only the
concurrency part of the active execution plan; marshalling stays \code{arrow_r} or
\code{arrow_c} according to \code{\link[=rducks_set_execution_plan]{rducks_set_execution_plan()}}.
}
\details{
Register UDFs while the connection is in the registration-safe
configuration, then call \code{rducks_enable_inproc()} before running queries that
should use the queued in-process path. Use \code{threads}/\code{external_threads} here
to adjust DuckDB's thread settings for queued execution.
}
