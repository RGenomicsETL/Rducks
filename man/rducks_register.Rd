% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/register.R
\name{rducks_register}
\alias{rducks_register}
\title{Register an R UDF in DuckDB}
\usage{
rducks_register(
  con,
  name,
  fun,
  args,
  returns,
  mode = c("row", "nanoarrow_lapply", "arrow_nanoarrow"),
  null_handling = c("default", "special"),
  exception_handling = c("rethrow", "return_null"),
  side_effects = FALSE
)
}
\arguments{
\item{con}{A \code{duckdb_connection}.}

\item{name}{SQL function name.}

\item{fun}{R function.}

\item{args}{Argument type specification. Use exported DuckDB-style type
objects such as \code{INTEGER}, \code{DOUBLE}, \code{INTEGER[]}, \code{INTEGER[3]},
\code{STRUCT(a = INTEGER)}, or \code{MAP(VARCHAR, INTEGER)}.}

\item{returns}{Return type specification.}

\item{mode}{Registration mode. \code{"row"} is implemented now and calls the R
function once per row through the native Rducks DuckDB extension.
\code{"nanoarrow_lapply"} and \code{"arrow_nanoarrow"} are reserved for future batch
UDF paths.}

\item{null_handling}{Either \code{"default"} for NULL-in/NULL-out without calling
the R function, or \code{"special"} to call the R function with NA-like R
values for NULL inputs.}

\item{exception_handling}{Either \code{"rethrow"} to report R errors to DuckDB, or
\code{"return_null"} to turn callback errors into SQL NULL values.}

\item{side_effects}{Logical scalar. Use \code{TRUE} for callbacks with randomness,
counters, I/O, mutation, or other side effects so DuckDB does not treat the
function as pure.}
}
\value{
Object of class \code{rducks_registration}. Keep this object if you want
to soft-unregister the UDF later with \code{\link[=rducks_unregister]{rducks_unregister()}}.
}
\description{
Registers a scalar R function as a DuckDB SQL function using the loaded Rducks
extension. The current implemented path is direct main-R-thread callback
execution and requires single-thread DuckDB execution.
}
