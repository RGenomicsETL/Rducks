% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/register.R
\name{rducks_register}
\alias{rducks_register}
\title{Register an R UDF in DuckDB}
\usage{
rducks_register(
  con,
  name,
  fun,
  args,
  returns,
  mode = "scalar",
  eval_mode = NULL,
  null_handling = c("default", "special"),
  exception_handling = c("rethrow", "return_null"),
  side_effects = FALSE
)
}
\arguments{
\item{con}{A \code{duckdb_connection}.}

\item{name}{SQL function name.}

\item{fun}{R function.}

\item{args}{Argument type specification. Use exported DuckDB-style type
objects such as \code{INTEGER}, \code{DOUBLE}, \code{INTEGER[]}, \code{INTEGER[3]},
\code{STRUCT(a = INTEGER)}, or \code{MAP(VARCHAR, INTEGER)}.}

\item{returns}{Return type specification.}

\item{mode}{Registration mode. \code{"scalar"} calls the R function once per
DuckDB row. \code{"vectorized"} calls the R function once per DuckDB chunk with
one R vector/list-column per declared argument.}

\item{eval_mode}{Compatibility override for the marshalling evaluator. Use
\code{NULL} to select from the active connection \code{rducks_execution_plan()}.
\code{"R"} maps to \code{arrow_r}; \code{"RC"} maps to \code{arrow_c} for scalar mode.
\code{mode = "vectorized"} currently supports \code{eval_mode = "R"} only because
\code{arrow_c} vectorized execution is not implemented yet.}

\item{null_handling}{Either \code{"default"} for NULL-in/NULL-out without calling
the R function, or \code{"special"} to call the R function with the declared
type's missing-value shape for NULL inputs (for example typed \code{NA} for
ordinary scalar types and \code{NULL} for exact/exotic, binary, and composite
values).}

\item{exception_handling}{Either \code{"rethrow"} to report R errors to DuckDB, or
\code{"return_null"} to turn R errors into SQL NULL values.}

\item{side_effects}{Logical scalar. Use \code{TRUE} for functions with randomness,
counters, I/O, mutation, or other side effects so DuckDB does not treat the
function as pure.}
}
\value{
Object of class \code{rducks_registration} containing the connection,
normalized signature, and registration options. The UDF remains registered
in DuckDB even if this object is discarded.
}
\description{
Registers an R function as a DuckDB SQL function using the loaded Rducks
extension. Registration requires \code{external_threads=1} plus
\verb{PRAGMA threads=1} so native registration and the default scalar execution
path stay on the calling R thread. After registration, use
\code{\link[=rducks_enable_inproc]{rducks_enable_inproc()}} to opt into queued same-process execution.
}
