% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/duckplyr.R
\name{rducks_with_duckplyr}
\alias{rducks_with_duckplyr}
\alias{with.duckdb_connection}
\title{Evaluate a duckplyr pipeline with dynamic Rducks scalar UDFs}
\usage{
rducks_with_duckplyr(
  con,
  expr,
  returns,
  env = parent.frame(),
  null_handling = c("default", "special"),
  exception_handling = c("rethrow", "return_null"),
  side_effects = FALSE
)

\method{with}{duckdb_connection}(data, expr, ..., rducks_returns, rducks_env = parent.frame())
}
\arguments{
\item{con}{A \code{duckdb_connection} with Rducks enabled.}

\item{expr}{A duckplyr expression or pipeline to evaluate.}

\item{returns}{Named list or named character vector of return types. Names
must be R function names visible from \code{env}; values are Rducks type
descriptors or scalar type tokens, e.g. \code{list(score_fun = DOUBLE)}.}

\item{env}{Evaluation environment for \code{expr} and function lookup.}

\item{null_handling, exception_handling, side_effects}{Passed to
\code{\link[=rducks_register_scalar_udf]{rducks_register_scalar_udf()}}.}

\item{data}{A \code{duckdb_connection} with Rducks enabled.}

\item{...}{Reserved for future extensions; must be empty.}

\item{rducks_returns}{Named return-type list for dynamic Rducks UDFs.}

\item{rducks_env}{Evaluation environment for \code{expr} and function lookup.}
}
\value{
The value of the evaluated expression.
}
\description{
Registers selected R functions as dynamic-argument Rducks scalar UDFs on a
DuckDB connection, rewrites matching calls in a captured duckplyr expression
to duckplyr's DuckDB-function escape hatch, and evaluates the rewritten
expression. This lets a duckplyr pipeline stay in DuckDB for those calls
instead of falling back to dplyr, provided every registered function has an
explicit return type.
}
\details{
This helper intentionally requires return-type declarations: DuckDB needs a
scalar function's return type during planning even when its input arguments
are accepted dynamically. Dynamic arguments are a duckplyr-oriented
convenience path that uses nanoarrow's default input conversion. The duckplyr
bridge registers these UDFs with \code{mode = "scalar"}; it does not expose
Rducks' vectorized chunk-call mode. Use explicit \code{args} in
\code{\link[=rducks_register_scalar_udf]{rducks_register_scalar_udf()}}
when you need Rducks' declared composite, exotic, special-NULL, or vectorized
input semantics.
}
